# graphlane
Every agent stays in its lane. A .claude/ setup that runs Claude Code subagents as a fixed pipeline — research, design, human approval, build, review — with hooks that block instead of hope.
